# Node_with_SQL2
Summary MySQL + Node.js + Faker.js

Faker.js helps to generate fake data

mysql2 helps to insert data from Node.js

Use VALUES ? for bulk inserts

Use TRUNCATE to delete all data and reset IDs

Use full file paths in source command

🚀 Setup MySQL in Terminal
cd "C:\Program Files\MySQL\MySQL Server 8.0\bin"
./mysql -u root -p

